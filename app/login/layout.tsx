import { ReactNode } from "react";

export const metadata = {
  title: "Login - Le ture Panel",
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
